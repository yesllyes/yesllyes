import styled from 'styled-components';

const StyledUserPost = styled.section`
  display: flex;
  justify-content: space-between;
  align-items: center;

  & > img {
    padding-right: 14px;
  }
`;

export default StyledUserPost;
